import { ref } from "@firebase/storage";
import { storage } from "./firebase";

// An example of how to use storage

// export const getFileRef = (userUid: string, fileId: string) =>
//   ref(storage, `users/${userUid}/${fileId}`);
